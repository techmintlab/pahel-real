# PAHEL FOUNDATION Admin Panel

Separate responsive React/Vite admin panel for the single seeded administrator. It consumes the FastAPI routes under `/api/admin`.

```bash
cd admin
npm install
cp .env.example .env
npm run dev
```

There is no public admin registration. Development login defaults are documented in `/app/memory/test_credentials.md`.