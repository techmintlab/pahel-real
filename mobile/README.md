# PAHEL FOUNDATION Mobile App

The Expo mobile application is kept in `/app/frontend` because that is the preconfigured Expo workspace used by the preview and Android tooling. This folder documents the mobile application boundary requested by the project brief; backend and admin remain separate siblings under `/app`.